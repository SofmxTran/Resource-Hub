<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">Resource Hub</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navMenu"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navMenu">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link <%= (currentPath || '') === '/' ? 'active' : '' %>" href="/">Home</a>
        </li>
        <% if (currentUser) { %>
        <li class="nav-item">
          <a class="nav-link <%= (currentPath || '') === '/dashboard' ? 'active' : '' %>" href="/dashboard">Dashboard</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= (currentPath || '').startsWith('/resources') ? 'active' : '' %>" href="/resources">My Resources</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= (currentPath || '') === '/profile' ? 'active' : '' %>" href="/profile">My Profile</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= (currentPath || '').startsWith('/domains') ? 'active' : '' %>" href="/domains">Domains</a>
        </li>
        <% if (currentUser.isAdmin) { %>
        <li class="nav-item">
          <a class="nav-link <%= (currentPath || '').startsWith('/admin') ? 'active' : '' %>" href="/admin">Admin</a>
        </li>
        <% } %>
        <% } %>
      </ul>
      <% if (currentUser) { %>
      <div class="d-flex align-items-center gap-3">
        <span class="text-white small"
          >Hi, <strong><%= currentUser.username %></strong></span
        >
        <form action="/logout" method="POST">
          <button class="btn btn-outline-light btn-sm">Logout</button>
        </form>
      </div>
      <% } else { %>
      <div class="ms-auto d-flex gap-2">
        <a class="btn btn-outline-light btn-sm" href="/login">Login</a>
        <a class="btn btn-light btn-sm" href="/register">Register</a>
      </div>
      <% } %>
    </div>
  </div>
</nav>

